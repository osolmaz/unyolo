{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brokerkit.io/schema/host-deployment/v1/deployment.schema.json",
  "title": "BrokerKit host deployment",
  "type": "object",
  "additionalProperties": false,
  "required": ["api_version", "name", "runtime", "agents", "operators", "components"],
  "properties": {
    "api_version": { "const": "brokerkit.io/host-deployment/v1" },
    "name": { "$ref": "#/$defs/name" },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": ["manifest", "signature", "public_key"],
      "properties": {
        "manifest": { "$ref": "#/$defs/reference" },
        "signature": { "$ref": "#/$defs/reference" },
        "public_key": { "$ref": "#/$defs/reference" }
      }
    },
    "agents": { "type": "array", "minItems": 1, "maxItems": 32, "items": { "$ref": "#/$defs/agent" } },
    "operators": { "type": "array", "minItems": 1, "maxItems": 32, "items": { "$ref": "#/$defs/operator" } },
    "components": { "type": "array", "minItems": 1, "maxItems": 32, "items": { "$ref": "#/$defs/component" } },
    "integrations": { "type": "array", "maxItems": 32, "items": { "$ref": "#/$defs/integration" } }
  },
  "$defs": {
    "name": { "type": "string", "pattern": "^[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?$" },
    "path": { "type": "string", "minLength": 1, "maxLength": 2048 },
    "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "reference": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "sha256"],
      "properties": { "path": { "$ref": "#/$defs/path" }, "sha256": { "$ref": "#/$defs/digest" } }
    },
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "client_id", "unix_user", "account_mode", "home", "shell", "component_ids"],
      "properties": {
        "id": { "$ref": "#/$defs/name" },
        "client_id": { "$ref": "#/$defs/name" },
        "unix_user": { "type": "string", "minLength": 1, "maxLength": 32 },
        "account_mode": { "enum": ["managed", "existing"] },
        "home": { "$ref": "#/$defs/path" },
        "shell": { "$ref": "#/$defs/path" },
        "component_ids": { "type": "array", "minItems": 1, "maxItems": 32, "uniqueItems": true, "items": { "$ref": "#/$defs/name" } }
      }
    },
    "operator": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "unix_user"],
      "properties": { "id": { "$ref": "#/$defs/name" }, "unix_user": { "type": "string", "minLength": 1, "maxLength": 32 } }
    },
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "profile"],
      "properties": { "id": { "$ref": "#/$defs/name" }, "profile": { "$ref": "#/$defs/reference" } }
    },
    "integration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "agent_id", "profile"],
      "properties": {
        "id": { "$ref": "#/$defs/name" },
        "kind": { "$ref": "#/$defs/name" },
        "agent_id": { "$ref": "#/$defs/name" },
        "profile": { "$ref": "#/$defs/reference" }
      }
    }
  }
}
