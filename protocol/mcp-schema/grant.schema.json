{
  "$comment": "Generated from protocol/openapi/mcp-v1.yaml; do not edit.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unyolo.io/schema/mcp/v1/grant.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "api_version",
    "id",
    "status",
    "operation",
    "target",
    "attrs",
    "mode",
    "minutes",
    "max_uses",
    "uses_remaining",
    "used_count",
    "pending_until",
    "expires_at"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "const": "unyolo.io/mcp-grant/v1"
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "active",
        "denied",
        "expired",
        "consumed",
        "revoked",
        "canceled",
        "retained"
      ]
    },
    "operation": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "target": {
      "type": "object",
      "minProperties": 1,
      "maxProperties": 16
    },
    "attrs": {
      "type": "object",
      "maxProperties": 32
    },
    "mode": {
      "type": "string",
      "enum": [
        "window",
        "execution"
      ]
    },
    "minutes": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10080
    },
    "max_uses": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 1,
      "maximum": 1000000
    },
    "uses_remaining": {
      "type": "integer",
      "minimum": -1
    },
    "used_count": {
      "type": "integer",
      "minimum": 0
    },
    "pending_until": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "expires_at": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    }
  },
  "$defs": {}
}
