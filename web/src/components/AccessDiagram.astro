---
/**
 * Two stacked scenarios on the landing page. The top one hands your credential
 * straight to the agent, so every wire out of it is live. The bottom one gives
 * the credential to the broker instead, so only the wire policy allows carries
 * current.
 *
 * Everything here is inline SVG plus CSS, so the diagram works with scripting
 * disabled.
 */

// The right-hand column models the real hierarchy. Two branches sit inside
// acme/api, which is itself a target because the repository can be deleted.
// acme/secrets is a separate repository.
const REPO_BOX = { x: 596, y: 16, w: 296, h: 146 };

const targets = [
  // `safe` is the work you actually wanted; it behaves the same in both rows.
  { id: "branch", y: 90, endX: REPO_BOX.x, name: "agent-a/parser-fix", note: "branch", indent: true, safe: true },
  { id: "main", y: 136, endX: REPO_BOX.x, name: "main", note: "default branch", indent: true },
  { id: "repo", y: 44, endX: REPO_BOX.x, name: "acme/api", note: "repository" },
  { id: "secrets", y: 208, endX: 608, name: "acme/secrets", note: "private repo" },
];

/* ---- icons -------------------------------------------------------------

   Cube faces sit on a 2:1 axonometric grid, so the face planes slope by
   atan(0.5) = 26.565 degrees. Anything drawn onto a face is skewed by that
   same angle so it lies in the plane rather than looking pasted on.

   The person is built from smooth solids instead: a sphere on a cone.
   ---------------------------------------------------------------------- */

const SKEW_RIGHT = "skewY(-26.565)";

const personIcon = `
  <ellipse class="iso-shadow" cx="0" cy="33" rx="25" ry="6"/>
  <path fill="url(#gBody)" d="
    M -21,29
    C -21,5 -12,-7 0,-7
    C 12,-7 21,5 21,29
    A 21 6.5 0 0 1 -21,29 Z"/>
  <ellipse fill="url(#gBodyBase)" cx="0" cy="29" rx="21" ry="6.5"/>
  <circle fill="url(#gHead)" cx="0" cy="-23" r="13"/>
`;

const agentIcon = `
  <ellipse class="iso-shadow" cx="0" cy="35" rx="29" ry="7"/>
  <polygon fill="url(#gTop)"   points="-28,-14 0,-28 28,-14 0,0"/>
  <polygon fill="url(#gLeft)"  points="-28,-14 0,0 0,30 -28,16"/>
  <polygon fill="url(#gRight)" points="28,-14 28,16 0,30 0,0"/>
  <!-- Antenna rises from the centre of the top face, so it is drawn after the
       faces rather than behind them. -->
  <line x1="0" y1="-14" x2="0" y2="-34" class="iso-antenna"/>
  <circle class="iso-mark" cx="0" cy="-37" r="3.2"/>
  <g transform="translate(14 8) ${SKEW_RIGHT}">
    <rect class="iso-screen" x="-9.5" y="-9" width="19" height="15" rx="2.5"/>
    <circle class="iso-eye" cx="-4.2" cy="-2.2" r="2.3"/>
    <circle class="iso-eye" cx="4.2" cy="-2.2" r="2.3"/>
  </g>
`;

const brokerIcon = `
  <ellipse class="iso-shadow" cx="0" cy="39" rx="32" ry="7.5"/>
  <polygon fill="url(#gaTop)"   points="-30,-22 0,-38 30,-22 0,-6"/>
  <polygon fill="url(#gaLeft)"  points="-30,-22 0,-6 0,34 -30,18"/>
  <polygon fill="url(#gaRight)" points="30,-22 30,18 0,34 0,-6"/>
  <g transform="translate(15 6) ${SKEW_RIGHT}">
    <path class="iso-a-mark" d="M -6,-4 A 6 6 0 0 1 6,-4 L 6,0 L 3,0 L 3,-4 A 3 3 0 0 0 -3,-4 L -3,0 L -6,0 Z"/>
    <rect class="iso-a-mark" x="-7.5" y="0" width="15" height="12" rx="2"/>
  </g>
`;

const policyIcon = `
  <path fill="url(#gTop)" d="M -11,-13 L 5,-13 L 11,-7 L 11,13 L -11,13 Z"/>
  <path fill="url(#gLeft)" d="M 5,-13 L 11,-7 L 5,-7 Z"/>
  <rect class="iso-mark" x="-7" y="-5" width="14" height="1.6" rx="0.8"/>
  <rect class="iso-mark" x="-7" y="-0.5" width="14" height="1.6" rx="0.8"/>
  <rect class="iso-mark" x="-7" y="4" width="9" height="1.6" rx="0.8"/>
`;

const cubeIcon = `
  <ellipse class="iso-shadow" cx="0" cy="18" rx="17" ry="4.5"/>
  <polygon fill="url(#gTop)"   points="-17,-10 0,-19 17,-10 0,-1"/>
  <polygon fill="url(#gLeft)"  points="-17,-10 0,-1 0,16 -17,7"/>
  <polygon fill="url(#gRight)" points="17,-10 17,7 0,16 0,-1"/>
`;

/** Wire from a hub on the left to a target on the right. */
const wire = (fromX: number, y: number, endX: number) =>
  `M ${fromX} 130 C ${fromX + 110} 130, ${fromX + 150} ${y}, ${endX} ${y}`;

/**
 * Point on that cubic bezier at t, so a cut marker lands exactly on its wire
 * instead of being eyeballed. t = 0.6 is far enough along that the four wires
 * have fanned apart.
 */
function pointOnWire(fromX: number, y: number, endX: number, t = 0.6) {
  const p0 = { x: fromX, y: 130 };
  const p1 = { x: fromX + 110, y: 130 };
  const p2 = { x: fromX + 150, y };
  const p3 = { x: endX, y };
  const u = 1 - t;
  const a = u * u * u;
  const b = 3 * u * u * t;
  const c = 3 * u * t * t;
  const d = t * t * t;
  return {
    x: a * p0.x + b * p1.x + c * p2.x + d * p3.x,
    y: a * p0.y + b * p1.y + c * p2.y + d * p3.y,
  };
}
---

<div class="diagram">
  <svg class="diagram__defs" aria-hidden="true" focusable="false">
    <defs>
      <radialGradient id="gHead" cx="34%" cy="26%" r="78%">
        <stop offset="0" class="s-hi" />
        <stop offset="0.62" class="s-top" />
        <stop offset="1" class="s-left" />
      </radialGradient>
      <linearGradient id="gBody" x1="0" y1="0" x2="1" y2="0.25">
        <stop offset="0" class="s-top" />
        <stop offset="0.5" class="s-left" />
        <stop offset="1" class="s-right" />
      </linearGradient>
      <linearGradient id="gBodyBase" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" class="s-left" />
        <stop offset="1" class="s-right2" />
      </linearGradient>
      <linearGradient id="gTop" x1="0" y1="0" x2="0.6" y2="1">
        <stop offset="0" class="s-hi" />
        <stop offset="1" class="s-top2" />
      </linearGradient>
      <linearGradient id="gLeft" x1="0" y1="0" x2="0.3" y2="1">
        <stop offset="0" class="s-left" />
        <stop offset="1" class="s-left2" />
      </linearGradient>
      <linearGradient id="gRight" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" class="s-right" />
        <stop offset="1" class="s-right2" />
      </linearGradient>
      <linearGradient id="gaTop" x1="0" y1="0" x2="0.6" y2="1">
        <stop offset="0" class="s-a-top" />
        <stop offset="1" class="s-a-top2" />
      </linearGradient>
      <linearGradient id="gaLeft" x1="0" y1="0" x2="0.3" y2="1">
        <stop offset="0" class="s-a-left" />
        <stop offset="1" class="s-a-left2" />
      </linearGradient>
      <linearGradient id="gaRight" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" class="s-a-right" />
        <stop offset="1" class="s-a-right2" />
      </linearGradient>
    </defs>
  </svg>
  <!-- ------------------------------------------------------- loose agent -- -->
  <figure class="scenario scenario--risk">
    <figcaption class="scenario__caption">
      <span class="scenario__tag">Without a broker</span>
      <p class="scenario__line">
        You hand the token to the agent. It does the work you wanted, and the same token reaches
        everything else in your account.
      </p>
    </figcaption>

    <svg class="scenario__svg" viewBox="0 0 900 268" role="img" aria-labelledby="loose-title">
      <title id="loose-title">
        You give your GitHub token to the agent. It pushes the branch you wanted, and the same
        token also reaches the default branch, the acme/api repository itself, and a separate
        private repository.
      </title>

      {/* The work you wanted stays green in both rows; the rest is loose reach. */}
      {
        targets.map((t) => (
          <>
            <path class="wire wire--base" d={wire(330, t.y, t.endX)} />
            <path
              class={`wire ${t.safe ? "wire--live" : "wire--danger"}`}
              d={wire(330, t.y, t.endX)}
            />
            <circle
              class={`spark ${t.safe ? "spark--live" : "spark--danger"}`}
              r="3.5"
              style={`offset-path: path('${wire(330, t.y, t.endX)}')`}
            />
          </>
        ))
      }

      {/* you -> agent, carrying the token */}
      <path class="wire wire--base" d="M 132 130 L 270 130" />
      <path class="wire wire--danger" d="M 132 130 L 270 130" />
      <circle class="spark spark--danger" r="3.5" style="offset-path: path('M 132 130 L 270 130')" />
      <g class="token" transform="translate(201 130)">
        <rect x="-27" y="-11" width="54" height="22" rx="11" />
        <text x="0" y="4">token</text>
      </g>

      <g transform="translate(90 130)" set:html={personIcon} />
      <g transform="translate(300 130)" set:html={agentIcon} />
      <text class="node-label" x="90" y="196" text-anchor="middle">YOU</text>
      <text class="node-label" x="300" y="196" text-anchor="middle">AGENT</text>

      {/* acme/api frame: the two branches live inside the repository */}
      <rect
        class="repo-frame"
        x={REPO_BOX.x}
        y={REPO_BOX.y}
        width={REPO_BOX.w}
        height={REPO_BOX.h}
        rx="10"
      />
      <line class="repo-rule" x1={REPO_BOX.x + 14} y1="66" x2={REPO_BOX.x + REPO_BOX.w - 14} y2="66" />

      {
        targets.map((t) => (
          <>
            <g transform={`translate(${t.indent ? 646 : 622} ${t.y})`} set:html={cubeIcon} />
            <text class="res-name" x={t.indent ? 676 : 652} y={t.y - 1}>
              {t.name}
            </text>
            <text class="res-note" x={t.indent ? 676 : 652} y={t.y + 13}>
              {t.note}
            </text>
          </>
        ))
      }
    </svg>
  </figure>

  <!-- ------------------------------------------------------ brokered path -- -->
  <figure class="scenario scenario--safe">
    <figcaption class="scenario__caption">
      <span class="scenario__tag">With unYOLO</span>
      <p class="scenario__line">
        The broker holds the token. The agent asks for the same work, and the calls you never
        authorized are refused.
      </p>
    </figcaption>

    <svg class="scenario__svg" viewBox="0 0 900 268" role="img" aria-labelledby="brokered-title">
      <title id="brokered-title">
        You give your GitHub token to the broker. The agent sends requests to the broker, which
        checks them against your scope.json, allows the same branch push and returns its result,
        and refuses the other three.
      </title>

      {
        targets.map((t) => (
          <>
            <path class="wire wire--base" d={wire(400, t.y, t.endX)} />
            {t.safe ? (
              <>
                <path class="wire wire--live" d={wire(400, t.y, t.endX)} />
                <circle
                  class="spark spark--live"
                  r="3.5"
                  style={`offset-path: path('${wire(400, t.y, t.endX)}')`}
                />
              </>
            ) : (
              <g
                class="cut"
                transform={`translate(${pointOnWire(400, t.y, t.endX).x.toFixed(1)} ${pointOnWire(400, t.y, t.endX).y.toFixed(1)})`}
              >
                <line x1="-7" y1="-7" x2="7" y2="7" />
                <line x1="7" y1="-7" x2="-7" y2="7" />
              </g>
            )}
          </>
        ))
      }

      {/* you -> broker, carrying the token */}
      <path class="wire wire--base" d="M 128 78 C 210 88, 250 100, 322 118" />
      <path class="wire wire--live" d="M 128 78 C 210 88, 250 100, 322 118" />
      <g class="token" transform="translate(216 96)">
        <rect x="-27" y="-11" width="54" height="22" rx="11" />
        <text x="0" y="4">token</text>
      </g>

      {/* agent -> broker, carrying only a request */}
      <path class="wire wire--base" d="M 128 190 C 210 180, 250 166, 322 144" />
      <path class="wire wire--request" d="M 128 190 C 210 180, 250 166, 322 144" />
      <g class="token token--muted" transform="translate(214 166)">
        <rect x="-33" y="-11" width="66" height="22" rx="11" />
        <text x="0" y="4">request</text>
      </g>

      {/* broker -> agent, returning the result of the allowed call */}
      <path class="wire wire--base" d="M 326 158 C 254 190, 214 206, 130 212" />
      <path class="wire wire--live" d="M 326 158 C 254 190, 214 206, 130 212" />
      <circle
        class="spark spark--live"
        r="3.2"
        style="offset-path: path('M 326 158 C 254 190, 214 206, 130 212')"
      />
      <g class="token token--muted" transform="translate(224 202)">
        <rect x="-27" y="-11" width="54" height="22" rx="11" />
        <text x="0" y="4">result</text>
      </g>

      {/* the policy the broker checks against */}
      <path class="wire wire--base" d="M 370 66 L 370 90" />
      <g transform="translate(370 44)" set:html={policyIcon} />
      <text class="res-note" x="370" y="70" text-anchor="middle">scope.json</text>

      <g transform="translate(88 74)" set:html={personIcon} />
      <g transform="translate(88 190)" set:html={agentIcon} />
      <g transform="translate(370 130)" set:html={brokerIcon} />
      <text class="node-label" x="88" y="130" text-anchor="middle">YOU</text>
      <text class="node-label" x="88" y="250" text-anchor="middle">AGENT</text>
      <text class="node-label" x="370" y="200" text-anchor="middle">BROKER</text>

      {/* acme/api frame: the two branches live inside the repository */}
      <rect
        class="repo-frame"
        x={REPO_BOX.x}
        y={REPO_BOX.y}
        width={REPO_BOX.w}
        height={REPO_BOX.h}
        rx="10"
      />
      <line class="repo-rule" x1={REPO_BOX.x + 14} y1="66" x2={REPO_BOX.x + REPO_BOX.w - 14} y2="66" />

      {
        targets.map((t) => (
          <>
            <g transform={`translate(${t.indent ? 646 : 622} ${t.y})`} set:html={cubeIcon} class:list={[!t.safe && "is-dim"]} />
            <text class:list={["res-name", !t.safe && "is-dim"]} x={t.indent ? 676 : 652} y={t.y - 1}>
              {t.name}
            </text>
            <text class="res-note" x={t.indent ? 676 : 652} y={t.y + 13}>
              {t.note}
            </text>
          </>
        ))
      }
    </svg>
  </figure>
</div>

<style>
  .diagram {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .scenario {
    margin: 0;
    padding: 1.25rem 1.4rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--bg);
  }

  .scenario--risk {
    border-color: var(--danger-border);
  }

  .scenario__caption {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.5rem 0.9rem;
    margin-bottom: 0.35rem;
  }

  .scenario__tag {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--text-faint);
  }

  .scenario--risk .scenario__tag {
    color: var(--danger-text);
  }

  .scenario__line {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.5;
    color: var(--text-muted);
  }

  .scenario__svg {
    display: block;
    width: 100%;
    height: auto;
    overflow: visible;
  }

  /* ------------------------------------------------------------- shapes -- */

  .diagram__defs {
    position: absolute;
    width: 0;
    height: 0;
  }

  .s-hi { stop-color: var(--iso-hi); }
  .s-top { stop-color: var(--iso-top); }
  .s-top2 { stop-color: var(--iso-top2); }
  .s-left { stop-color: var(--iso-left); }
  .s-left2 { stop-color: var(--iso-left2); }
  .s-right { stop-color: var(--iso-right); }
  .s-right2 { stop-color: var(--iso-right2); }
  .s-a-top { stop-color: var(--iso-a-top); }
  .s-a-top2 { stop-color: var(--iso-a-top2); }
  .s-a-left { stop-color: var(--iso-a-left); }
  .s-a-left2 { stop-color: var(--iso-a-left2); }
  .s-a-right { stop-color: var(--iso-a-right); }
  .s-a-right2 { stop-color: var(--iso-a-right2); }

  .scenario__svg :global(.iso-mark) {
    fill: var(--iso-mark);
  }
  .scenario__svg :global(.iso-shadow) {
    fill: var(--iso-shadow);
  }
  .scenario__svg :global(.iso-screen) {
    fill: var(--iso-screen);
  }
  .scenario__svg :global(.iso-eye) {
    fill: var(--iso-eye);
  }
  .scenario__svg :global(.iso-antenna) {
    stroke: var(--iso-mark);
    stroke-width: 2;
    stroke-linecap: round;
  }
  .scenario__svg :global(.iso-a-top) {
    fill: var(--iso-a-top);
  }
  .scenario__svg :global(.iso-a-left) {
    fill: var(--iso-a-left);
  }
  .scenario__svg :global(.iso-a-right) {
    fill: var(--iso-a-right);
  }
  .scenario__svg :global(.iso-a-mark) {
    fill: var(--iso-a-mark);
  }

  .scenario__svg :global(.is-dim) {
    opacity: 0.4;
  }

  .node-label {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.09em;
    fill: var(--text-faint);
  }

  .repo-frame {
    fill: none;
    stroke: var(--border);
    stroke-width: 1.5;
    rx: 10;
  }

  .repo-rule {
    stroke: var(--border);
    stroke-width: 1;
  }

  .res-name {
    font-family: var(--font-mono);
    font-size: 13px;
    fill: var(--text-strong);
  }

  .res-note {
    font-family: var(--font-sans);
    font-size: 10.5px;
    fill: var(--text-faint);
  }

  .res-name.is-dim {
    fill: var(--text-faint);
  }

  /* -------------------------------------------------------------- wires -- */

  .wire {
    fill: none;
    stroke-linecap: round;
  }

  .wire--base {
    stroke: var(--border-strong);
    stroke-width: 1.5;
  }

  /* Marching dashes read as current moving along the wire. */
  .wire--danger,
  .wire--live,
  .wire--request {
    stroke-width: 2;
    stroke-dasharray: 5 9;
    animation: current 0.85s linear infinite;
  }

  .wire--danger {
    stroke: var(--wire-danger);
    animation-duration: 0.55s;
  }

  .wire--live {
    stroke: var(--wire-live);
  }

  .wire--request {
    stroke: var(--text-faint);
    animation-duration: 1.3s;
  }

  @keyframes current {
    to {
      stroke-dashoffset: -14;
    }
  }

  /* A brighter pulse riding the same path. */
  .spark {
    offset-rotate: 0deg;
    animation: travel 2.1s linear infinite;
  }

  .spark--danger {
    fill: var(--wire-danger);
    animation-duration: 1.5s;
  }

  .spark--live {
    fill: var(--wire-live);
  }

  @keyframes travel {
    from {
      offset-distance: 0%;
      opacity: 0;
    }
    12% {
      opacity: 1;
    }
    88% {
      opacity: 1;
    }
    to {
      offset-distance: 100%;
      opacity: 0;
    }
  }

  .cut line {
    stroke: var(--danger-text);
    stroke-width: 2.25;
    stroke-linecap: round;
  }

  .token rect {
    fill: var(--bg);
    stroke: var(--border-strong);
    stroke-width: 1;
  }

  .token text {
    font-family: var(--font-mono);
    font-size: 10.5px;
    text-anchor: middle;
    fill: var(--text-muted);
  }

  .scenario--risk .token rect {
    stroke: var(--danger-border);
  }

  .scenario--risk .token text {
    fill: var(--danger-text);
  }

  .token--muted rect {
    stroke: var(--border);
  }

  /* --------------------------------------------------------------- misc -- */

  @media (max-width: 46rem) {
    .scenario {
      padding: 1rem 0.9rem 0.75rem;
    }
    .scenario__svg {
      min-width: 520px;
    }
    .scenario > svg {
      display: block;
    }
    .scenario {
      overflow-x: auto;
    }
  }
</style>
