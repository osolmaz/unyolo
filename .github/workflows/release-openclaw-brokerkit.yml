name: Release openclaw-brokerkit

on:
  release:
    types:
      - published
  workflow_dispatch:

concurrency:
  group: release-openclaw-brokerkit-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  npm:
    name: Publish npm package
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'openclaw-brokerkit/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11.11.0

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - run: npm install --global npm@latest
      - run: pnpm install --frozen-lockfile

      - name: Validate release metadata
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          node - <<'NODE'
          const { readFileSync } = require("node:fs");

          const pkg = JSON.parse(readFileSync("plugins/openclaw/package.json", "utf8"));
          const errors = [];

          if (pkg.name !== "openclaw-brokerkit") {
            errors.push(`package name must be openclaw-brokerkit; found ${pkg.name ?? "<missing>"}`);
          }
          if (pkg.private === true) {
            errors.push("package must not be private");
          }
          if (!pkg.version) {
            errors.push("package version is missing");
          }
          if (process.env.EVENT_NAME === "release") {
            const expected = `openclaw-brokerkit/v${pkg.version}`;
            if (process.env.RELEASE_TAG !== expected) {
              errors.push(`release tag must be ${expected}; found ${process.env.RELEASE_TAG}`);
            }
          }

          if (errors.length > 0) {
            for (const error of errors) console.error(error);
            process.exit(1);
          }

          console.log(`Release metadata valid for ${pkg.name}@${pkg.version}`);
          NODE

      - name: Ensure version is not already published
        working-directory: plugins/openclaw
        run: |
          set -euo pipefail
          package_version="$(node -p "require('./package.json').version")"
          published_version="$(npm view "openclaw-brokerkit@$package_version" version 2>/dev/null || true)"

          if [ "$published_version" = "$package_version" ]; then
            echo "openclaw-brokerkit@$package_version is already published on npm." >&2
            exit 1
          fi

          echo "Publishing openclaw-brokerkit@$package_version"

      - run: pnpm --filter openclaw-brokerkit check
      - run: pnpm --filter openclaw-brokerkit test
      - run: pnpm --filter openclaw-brokerkit build
      - run: pnpm --filter openclaw-brokerkit exec playwright install --with-deps
      - run: pnpm --filter openclaw-brokerkit test:browser
      - run: pnpm --filter openclaw-brokerkit test:package

      - name: Inspect package contents
        working-directory: plugins/openclaw
        run: npm pack --dry-run

      - name: Publish to npm
        working-directory: plugins/openclaw
        run: npm publish --access public --provenance
